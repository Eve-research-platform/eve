function formatRemaining(ms){const total=Math.max(0,Math.ceil(ms/1000));return `${Math.floor(total/60)}:${String(total%60).padStart(2,'0')}`}
async function renderStatus(){const el=document.getElementById('status');try{const response=await chrome.runtime.sendMessage({type:'EVE_NAV_GET_STATUS'});const task=response?.task;if(!task){el.innerHTML='<div class="eyebrow">Ready</div><p>No active navigation task. Start a Navigation task from Eve and the overlay will appear on the tested website.</p>';return}const remaining=task.expiresAt?`<div class="timer">${formatRemaining(task.expiresAt-Date.now())} remaining</div>`:'<div class="timer">No time limit</div>';el.innerHTML=`<div class="eyebrow">Active task</div><div class="task"></div><p>The Eve overlay is attached to the task tab.</p>${remaining}`;el.querySelector('.task').textContent=task.instructions||'Navigation task'}catch{el.innerHTML='<div class="eyebrow">Unavailable</div><p>The companion could not read its current task state.</p>'}}
renderStatus();
